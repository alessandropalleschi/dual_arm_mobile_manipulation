after source the setup.bash, run from terminal

roslaunch iliad sim.launch

The simulation will be opened in paused mode. 

Then run "init_sim.m" in script/ folder

Finally run Simulink file "controller_stateflow.slx" in script/ folder